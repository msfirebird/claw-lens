# ---- 第一道防线由 package.json "files" 白名单负责 ----
# 以下是第二道防线：在放行的目录里再排除不需要的细节

# bin/ 里的 TypeScript 源文件（编译产物在 dist/bin/ 里）
bin/*.ts

# dist/ 里的 source map（调试用，发布包不需要）
**/*.map

# 数据库文件（安全兜底）
*.db
*.db-shm
*.db-wal

# 环境配置（安全兜底，绝对不能进发布包）
.env
.env.local
.env.*.local

# 系统 / 编辑器文件
.DS_Store
.claude/
